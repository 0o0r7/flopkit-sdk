# MCP setup

Run the server locally with:

```bash
FLOPKIT_IDENTITY=identity.pem FLOPKIT_PASSPHRASE='set-in-your-environment' python -m flopkit.mcp_server
```

The server exposes identity, Technocore onboarding, message signing and verification, contribution logging, and proof export tools.
